---
const { class: classes, ...rest } = Astro.props;
---

<div {...rest} class={`flex flex-col md:flex-row items-start md:items-end justify-between pb-6 md:pb-8 lg:pb-10 ${classes ? classes : ""}`}>
	<slot />
</div>
