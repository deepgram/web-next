---
import Link from "../general/Link.astro";

const { title, class: classes, ...rest } = Astro.props;
---

<div class="flex flex-col md:flex-row items-start md:items-end justify-between pb-6 md:pb-8 lg:pb-10">
	<slot />
	<Link {...rest} class="text-lg font-semibold stroke-[3]" icon="arrow-right" iconSuffix>View All {title}</Link>
</div>
