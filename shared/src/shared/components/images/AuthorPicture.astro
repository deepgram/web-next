---
import ImageSrcSet from "./ImageSrcSet.astro"
const { author, class: classes } = Astro.props;
---

<ImageSrcSet filename={author.content.picture.filename} class={classes} alt={`Author photo for ${author.content.title}`} title={author.content.title} />
