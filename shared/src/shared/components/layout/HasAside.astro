---
const { class: classes, gridClass } = Astro.props;
---

<div class={`relative ${classes ? classes : ""} ${gridClass ? gridClass : "grid grid-cols-1 gap-4 lg:gap-6 lg:grid-cols-6 xl:grid-cols-4"}`}>
	<div><slot name="aside" /></div>
	<div class="lg:col-span-5 xl:col-span-3"><slot /></div>
</div>
