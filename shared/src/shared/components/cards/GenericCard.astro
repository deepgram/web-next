---
import Link from "../general/Link.astro";

const { class: classes, href, ...rest } = Astro.props;
---

<Link href={href} class={`block text-white ${classes ? classes : ""}`} {...rest}>
	<slot />
</Link>
