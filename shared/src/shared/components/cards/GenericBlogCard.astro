---
import GenericCard from "./GenericCard.astro";

const { class: classes, ...rest } = Astro.props;
---

<GenericCard class={`min-w-[10rem] bg-mist/50 shadow-dg bg-blur ${classes ? classes : ""}`} {...rest}>
	<slot />
</GenericCard>
