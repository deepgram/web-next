---
import GenericCard from "./GenericCard.astro";

const { class: classes, ...rest } = Astro.props;
---

<GenericCard class={`min-w-[18rem] bg-mist/50 shadow-dg ${classes ? classes : ""}`} {...rest}>
	<slot />
</GenericCard>
