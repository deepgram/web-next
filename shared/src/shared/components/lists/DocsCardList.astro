---
const { class: classes, gridClass, ...rest } = Astro.props;
---

<div class={`w-full ${classes ? classes : ""}`} {...rest}>
	<div class="relative mx-auto -mr-0 xl:-mr-64">
		<slot name="title" />
		<!-- <div class={`${gridClass === 'threeColumn' ? 'grid-cols-1 sm:grid-cols-3' : 'grid-cols-1 sm:grid-cols-2'} grid gap-8 sm:gap-4`}>
			<slot />
		</div> -->
		<div class="grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 grid gap-8 sm:gap-4">
			<slot />
		</div>
	</div>
</div>