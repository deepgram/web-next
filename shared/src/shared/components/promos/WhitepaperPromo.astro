---
import AbstractPromo from "./AbstractPromo.astro";
import PromoForm from "./PromoForm.astro";

const { date, backgroundImage, theme, formProps, buttonText, ...rest } = Astro.props;
---

<AbstractPromo strapline="White Paper" title="this is the title" backgroundImage={backgroundImage} theme={theme} {...rest}>
	<PromoForm theme={theme} formProps={formProps} buttonText={buttonText} />
</AbstractPromo>
