---
import AbstractPromo from "./AbstractPromo.astro";
import PromoForm from "./PromoForm.astro";

const { date, backgroundImage, theme, formText, ...rest } = Astro.props;
---

<AbstractPromo strapline="White Paper" title="this is the title" backgroundImage={backgroundImage} {...rest}>
	<PromoForm theme={theme} formText={formText} />
</AbstractPromo>
