---
import AbstractPromo from "./AbstractPromo.astro";
import PromoLink from "./PromoLink.astro";

const { date, title, strapline, straplineColor, backgroundImage, backgroundColor, textColor, labelColor, placeholderColor, ...rest } = Astro.props;
---

<AbstractPromo
	title={title}
	strapline={strapline}
	straplineColor={straplineColor}
	backgroundColor={backgroundColor}
	textColor={textColor}
	backgroundImage={backgroundImage}
	{...rest}
>
	form here
	<PromoLink linkText="Register" />
</AbstractPromo>
