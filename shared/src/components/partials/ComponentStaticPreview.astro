---
import PreviewToggle from "./PreviewToggle.astro";

const { class: classes, ...rest } = Astro.props;
---

<PreviewToggle class={`border p-5 rounded-lg ${classes ? classes : null}`} {...rest}>
	<slot />
</PreviewToggle>
