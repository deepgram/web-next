{
  "private": true,
  "scripts": {
    "prebuild": "yarn content",
    "build": "nuxt build",
    "predev": "yarn clean && yarn content",
    "dev": "nuxt dev",
    "pregenerate": "yarn content",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "clean": "rm -rf ./content/",
    "content": "cp -r ../content/www/ ./content/"
  },
  "devDependencies": {
    "@nuxt/content": "npm:@nuxt/content-edge@latest",
    "nuxt": "^3.0.0-rc.3"
  }
}
