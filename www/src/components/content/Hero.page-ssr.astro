---
const { strap, title, body, primaryCta, secondaryCta, tertiaryCta } = Astro.props
---

<section class="hero">
  HERO CENTERED
  <span>{strap}</span>
  <h1>{title}</h1>
  <p>{body}</p>
  <p>{primaryCta}</p>
  <p>{secondaryCta}</p>
  <p>{tertiaryCta}</p>
</section>