---
import HeroCentered from "./hero/HeroCentered.astro";
const { promo, title, body, primaryCta, secondaryCta, tertiaryCta } = Astro.props
---

<section class="hero">
  <HeroCentered promo={promo} title={title} page="home"/>
  <!-- <span>{strap}</span>
  <h1>{title}</h1>
  <p>{body}</p>
  <p>{primaryCta}</p>
  <p>{secondaryCta}</p>
  <p>{tertiaryCta}</p> -->
</section>