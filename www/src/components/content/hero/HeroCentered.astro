---
import Promo from '../../../../../core-components/components/promo/Promo.astro';
const { strap, title, body, primaryCta, secondaryCta, tertiaryCta } = Astro.props
---

<section class="hero">
  <Promo>See how Deepgram stacks up on the ASR Comparison Tool.🍎🍊</Promo>
  <h1>{title}</h1>
  <p>{body}</p>
  <p>{primaryCta}</p>
  <p>{secondaryCta}</p>
  <p>{tertiaryCta}</p>
</section>