---
import Layout from "../../../layouts/OnPrem.astro";
import DocsCardList from "../../../shared/components/lists/DocsCardList.astro";
import DocsCard from "../../../shared/components/cards/DocsCard.astro";

const baseComponents = await Astro.glob("./**/index.mdx");

const orderedComponents = baseComponents.sort((a,b)=>{
	return a.frontmatter.order - b.frontmatter.order
})
---

<Layout>
	<article>
		<h1>Base Components</h1>
		<DocsCardList>
			{orderedComponents.map((guide) => <DocsCard class={`w-full flex flex-col`} doc={guide} />)}
		</DocsCardList>
	</article>
</Layout>

<style is:global>
	.post-prose h1 {
		/* margin-bottom: 2.5rem !important; */
		@apply mb-10;
	}
	h2 {
		@apply mb-5 mt-10;
		color: #111827;
		font-weight: 700;
		font-size: 1.5em;
		line-height: 1.3333333;
		font-family: Favorit;
	}

	p {
		font-weight: 400;
	}
</style>
