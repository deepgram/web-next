---
layout: ../../layouts/Documentation.astro
title: Dataset
description: Collection of resources that provide a large, representative sample
  of data that can be transcribed, labeled, and then used to train a model.
  Datasets can contain multiple resources, and you can add resources to a
  dataset, then retrain your model to get better results.
shortDescription: Collection of resources that provide a large, representative
  sample of data that can be transcribed, labeled, and then used to train a
  model. Datasets can contain multiple resources, and you can add resources to a
  dataset, then retrain your model to get better results.
---
